# Quality and security hardening

**Complexity:** ⚠️ **Medium** — targeted cross-stack changes with dependency and runtime validation.

## Objective

Make the current Module 1–2 application buildable, lintable and safer to run without changing its user-facing RAG scope.

## Tasks

1. Fix the TypeScript errors in transient chat messages and remove unused frontend imports.
   - **Validation:** `npm run build` succeeds.
2. Add a reproducible ESLint setup for TypeScript/React files.
   - **Validation:** `npm run lint` succeeds with zero warnings.
3. Replace unsigned JWT decoding with server-side Supabase token validation.
   - **Validation:** backend import succeeds with safe placeholder configuration; unauthenticated/invalid-token paths are covered by a focused test.
4. Pin backend dependency ranges that are compatible with the application’s OpenAI-compatible client integration.
   - **Validation:** a clean virtual environment installs `requirements.txt` without resolver backtracking failure.
5. Update the README to reflect the hardened authentication implementation and the available quality checks.
   - **Validation:** links/sections remain structurally valid and the documented commands are exercised.
